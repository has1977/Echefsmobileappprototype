import { PlaceholderPage } from '../../components/PlaceholderPage';
import { PlusCircle } from 'lucide-react';

export function WaiterOrderCreate() {
  return <PlaceholderPage title="Create Order" icon={<PlusCircle className="w-12 h-12 text-[#667c67]" />} />;
}
